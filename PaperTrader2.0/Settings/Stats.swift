//
//  Stats.swift
//  PaperTrader2.0
//
//  Created by Jimmy  on 2/1/22.
//

import Foundation


/*
 
 
 
 //pie chart - put this into infographic screen in the settings along with best gainers , losers, etc
 pieChart.delegate = self
 pieChart.frame = CGRect(x:0,y:self.view.bounds.size.height, width: self.view.frame.size.width, height:300)

 view.addSubview(pieChart)
 var entriesPie = [ChartDataEntry]()
 for x in 0..<10 {
     entriesPie.append(ChartDataEntry(x:Double(x),y:Double(x)))
 }
 let setPie = PieChartDataSet(entries:entriesPie)
 setPie.colors = ChartColorTemplates.material()
 let dataPie = PieChartData(dataSet:setPie)
 pieChart.data = dataPie
 */
